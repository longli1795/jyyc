[sap]
; 复制本文件为 secrets.ini 后再填。secrets.ini 已被 gitignore。
; 优先建议用：python -m app.sap_gui.set_sap_credential
; SAP Logon 里「名称」是 PRD，「系统描述」可能是 [p1 PRD，脚本会两个都试
connection = [p1 PRD
system = PRD
client = 800
language = ZH
user =
password =
